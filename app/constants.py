"""
Игровые константы.

Зачем отдельный файл:
    Дефолтные статы, пороги и лимиты нужны и в моделях, и в маршрутах,
    и в парсере ивентов. Если разбросать числа по коду — через месяц
    ты не вспомнишь, почему стартовые калории именно 300, и где их менять.

Правило:
    Никаких магических чисел в маршрутах. Всё — здесь.
"""

# ---- СТАРТОВЫЕ СТАТЫ ЭКСПЕДИЦИИ ----
# Баланс из математической модели: 300 калорий хватает на 15 дней
# при отряде в 10 человек (10 * 2 = 20 калорий в день).
DEFAULT_CALORIES = 300
DEFAULT_VIT_C = 100
DEFAULT_MORALE = 100
DEFAULT_WARMTH = 100
DEFAULT_SQUAD_SIZE = 10
DEFAULT_DAY = 1
DEFAULT_DISTANCE = 0

# ---- СТАРТОВЫЙ ИНВЕНТАРЬ ----
# Ланцет — для ампутаций. Соль — для засолки мяса.
# Без них игрок не сможет пройти первые ивенты «Железо и плоть»
# и «Цена соли».
DEFAULT_INVENTORY = {
    "salt": 1,
    "lancet": 1,
    "herbs": 3,
    "wood": 10,
}

# ---- ЛИМИТЫ СТАТОВ ----
# Статы не могут выйти за эти границы. Проверяется в парсере ивентов.
MAX_CALORIES = 300
MAX_VIT_C = 100
MAX_MORALE = 100
MAX_WARMTH = 100

# ---- ЦЕЛЬ ИГРЫ ----
TARGET_DISTANCE = 100  # вёрст до Мангазеи

# ---- ПОРОГИ КРИЗИСОВ ----
# Используются в движке ивентов и в математике хода.
SCURVY_STAGE_1 = 30   # ниже — цинга, скорость падает
SCURVY_STAGE_2 = 10   # ниже — люди умирают
MORALE_MUTINY = 20    # ниже — бунт
HUNGER_DEATH_DAYS = 3 # столько дней подряд при 0 калорий = смерть
